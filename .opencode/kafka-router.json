{
  "version": "003",
  "topics": ["opencode.prompts"],
  "dlqTopic": "opencode.dlq",
  "rules": [
    {
      "name": "default-prompt-rule",
      "jsonPath": "$",
      "promptTemplate": "Выполни задачу: ${$.task || $.prompt}",
      "agentId": "e2e-responder",
      "responseTopic": "opencode.responses",
      "timeoutMs": 120000,
      "concurrency": 1
    }
  ]
}